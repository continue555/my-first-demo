# 吹瓶机机械制造公司管理系统 - 操作文档

## 一、系统概述

本系统为吹瓶机机械制造公司量身定制的全流程管理系统，覆盖"签订合同 → 生产制造 → 验货入库 → 发货"全链路，实现部门协同、流程管控、时间追踪和数据导出。

---

## 二、部门结构

### 2.1 三大一级部门

| 部门ID | 部门名称 | 职责 |
|--------|----------|------|
| 1 | 销售部门 | 客户开发、合同签订、生产任务下达 |
| 2 | 生产部门 | 产品制造全流程管理（含5个子部门） |
| 3 | 财务部门 | 定金确认、尾款收款、成本核算 |

### 2.2 生产部门子结构

| 子部门ID | 子部门名称 | 上级部门 | 职责 |
|----------|-----------|----------|------|
| 4 | 技术部 | 生产部门 | 技术方案、图纸设计 |
| 5 | 采购部 | 生产部门 | 采购计划制定、物料采购 |
| 6 | 仓库 | 生产部门 | 物料入库、存储、配料 |
| 7 | 装配部 | 生产部门 | 产品组装 |
| 8 | 调试部 | 生产部门 | 产品调试与测试 |

---

## 三、角色与权限

### 3.1 角色定义

| 角色 | 标识 | 权限范围 |
|------|------|----------|
| 系统管理员 | admin | 全部权限，可查看所有数据 |
| 总经理 | management | 全部权限，可查看所有数据 |
| 销售 | sales | 合同签订、生产任务下达、查看订单进度 |
| 生产 | production | 生产制造相关流程操作 |
| 财务 | finance | 定金确认、收款确认、财务相关流程 |

### 3.2 预置账号

| 账号 | 密码 | 姓名 | 角色 | 所属部门 |
|------|------|------|------|----------|
| admin | 123456 | 系统管理员 | admin | - |
| zongjingli | 123456 | 总经理 | management | - |
| xiaoshou1 | 123456 | 张销售 | sales | 销售部门 |
| xiaoshou2 | 123456 | 李销售 | sales | 销售部门 |
| jishu1 | 123456 | 王技术 | production | 技术部 |
| caigou1 | 123456 | 赵采购 | production | 采购部 |
| cangku1 | 123456 | 钱仓库 | production | 仓库 |
| zhuangpei1 | 123456 | 孙装配 | production | 装配部 |
| tiaoshi1 | 123456 | 周调试 | production | 调试部 |
| caiwu1 | 123456 | 吴财务 | finance | 财务部门 |

---

## 四、工作流程

### 4.1 主流程（7个节点）

```
[1] 签订合同 → [2] 财务确认定金 → [3] 总经理签字 → [4] 销售下生产任务单
    → [5] 生产制造（含子流程） → [6] 验货入库 → [7] 发货
```

### 4.2 生产制造子流程（10个节点）

```
[5.1] 采购计划制定 → [5.2] 物料比对
    → ┌─ [5.3] 机架采购 ─┐
    → ├─ [5.4] 模架采购 ─┤
    → ├─ [5.5] 电气采购 ─┤→ [5.7] 物料进仓 → [5.8] 仓库配料 → [5.9] 装配 → [5.10] 调试
    → └─ [5.6] 外罩采购 ─┘
```

### 4.3 流程阶段定义

| 序号 | 阶段标识 | 阶段名称 | 负责部门 | 前置依赖 |
|------|----------|----------|----------|----------|
| 1 | contract_sign | 签订合同 | 销售部门 | - |
| 2 | deposit_confirm | 财务确认定金 | 财务部门 | contract_sign |
| 3 | gm_sign | 总经理签字 | 总经理 | deposit_confirm |
| 4 | production_order | 销售下生产任务单 | 销售部门 | gm_sign |
| 5.1 | purchase_plan | 采购计划制定 | 采购部 | production_order |
| 5.2 | material_check | 物料比对 | 采购部 | purchase_plan |
| 5.3 | purchase_frame | 机架采购 | 采购部 | material_check |
| 5.4 | purchase_mold_frame | 模架采购 | 采购部 | purchase_frame |
| 5.5 | purchase_electrical | 电气采购 | 采购部 | purchase_mold_frame |
| 5.6 | purchase_cover | 外罩采购 | 采购部 | purchase_electrical |
| 5.7 | material_in | 物料进仓 | 仓库 | purchase_cover |
| 5.8 | warehouse_prepare | 仓库配料 | 仓库 | material_in |
| 5.9 | assembly | 装配 | 装配部 | warehouse_prepare |
| 5.10 | debug | 调试 | 调试部 | assembly |
| 6 | inspection | 验货入库 | 仓库 | debug |
| 7 | shipping | 发货 | 销售部门 | inspection |

### 4.4 流程依赖规则

- **严格前置依赖**：每个节点的 `depends_on` 字段指定前置节点，只有前置节点状态为"已完成"时，当前节点才能开始
- **状态流转**：`pending(未开始)` → `in_progress(进行中)` → `completed(已完成)` / `delayed(延期)`
- **禁止跳过**：不允许从 `pending` 直接跳到 `completed`，必须先经过 `in_progress`
- **操作记录**：节点开始/完成时自动记录操作人ID和姓名

---

## 五、订单管理

### 5.1 订单编号规则

格式：`ORD-YYYY-MMDD-XXXX`（XXXX为4位随机数）

示例：`ORD-2026-0716-A3F2`

### 5.2 订单状态

| 状态 | 说明 |
|------|------|
| pending | 待处理（刚创建） |
| in_progress | 进行中 |
| completed | 已完成 |
| delayed | 已延期 |
| cancelled | 已取消 |

### 5.3 订单字段

| 字段 | 必填 | 说明 |
|------|------|------|
| 客户名称 | 是 | 签约客户公司名称 |
| 项目名称 | 是 | 项目/产品名称 |
| 产品型号 | 否 | 吹瓶机型号 |
| 数量 | 否 | 产品数量 |
| 合同金额 | 否 | 合同总金额 |
| 计划交货日期 | 是 | 合同约定的交货时间 |
| 备注 | 否 | 其他说明 |

---

## 六、时间管理

### 6.1 节点时间字段

| 字段 | 说明 |
|------|------|
| start_date | 节点实际开始时间 |
| planned_end_date | 计划完成时间 |
| actual_end_date | 实际完成时间 |

### 6.2 流程状态颜色标识

- **颜色标识**：
  - 🔵 蓝色 = 未开始（pending）
  - 🟠 橙色 = 进行中（in_progress）
  - 🟢 绿色 = 已完成（completed）
  - 🔴 红色 = 已延期（delayed）

---

## 七、数据导出

### 7.1 导出格式

Excel (.xlsx) 格式导出

### 7.2 导出内容

- 订单基本信息（编号、客户、项目、金额、交货日期）
- 完整流程节点信息（节点名称、开始时间、计划完成时间、实际完成时间、状态）
- 操作人记录
- 备注信息

### 7.3 导出范围

- 支持导出全部订单
- 支持按订单筛选导出
- 支持按状态筛选导出

---

## 八、数据筛选与搜索

- **订单搜索**：按订单编号、客户名称、项目名称模糊搜索
- **状态筛选**：按订单状态筛选
- **时间范围筛选**：按创建时间、交货时间范围筛选
- **部门筛选**：按负责部门筛选流程节点

---

## 九、通知机制

当流程节点完成时，系统自动通知下一节点的负责部门，确保工作衔接顺畅。

---

## 十、技术架构

| 层级 | 技术选型 |
|------|----------|
| 后端框架 | Node.js + Express |
| 数据库 | SQLite (better-sqlite3) |
| 共享定义 | shared/ 目录（流程阶段定义JSON，前后端共用） |
| 认证 | JWT Token |
| 密码加密 | bcryptjs |
| 前端框架 | Vue 3 + Vite |
| 状态管理 | Pinia |
| 路由 | Vue Router 4 |
| Excel导出 | xlsx 库 |

---

## 十一、页面结构

| 页面 | 路由路径 | 组件 | 功能 |
|------|----------|------|------|
| 登录页 | /login | Login.vue | 用户登录 |
| 布局页 | / | Layout.vue | 侧边栏导航与全局布局 |
| 仪表盘 | / | Dashboard.vue | 统计概览、最近订单、通知列表 |
| 订单列表 | /orders | Orders.vue | 订单管理、搜索筛选、批量导出 |
| 订单详情 | /orders/:id | OrderDetail.vue | 订单详情、流程节点操作 |
| 数据导出 | /export | Export.vue | Excel导出 |
| 通知中心 | /notifications | Notifications.vue | 通知列表、标记已读 |
| 用户管理 | /users | Users.vue | 用户增删改查、重置密码（管理员） |
| 操作日志 | /audit | AuditLog.vue | 操作记录查询（管理员） |

---

## 十二、前端架构说明

### 12.1 项目结构

```
frontend/
  src/
    main.js              # 应用入口，挂载Vue实例
    App.vue              # 根组件，包含Toast和Modal全局组件
    api/index.js         # API请求封装、流程阶段定义
    ../../shared/        # 共享数据目录（流程阶段定义 stage-defs.json）
    router/index.js      # Vue Router路由配置与导航守卫
    stores/
      auth.js            # 用户认证状态（Pinia）
      toast.js           # 全局消息提示状态
      modal.js           # 全局弹窗状态
    utils/
      index.js           # 公共工具函数（getOverdueInfo、statusText）
    components/
      AppToast.vue       # 全局Toast组件
      AppModal.vue       # 全局Modal组件
      OrderEditModal.vue # 订单编辑弹窗（公共组件）
    views/
      Login.vue          # 登录页
      Layout.vue         # 主布局（含侧边栏）
      Dashboard.vue      # 仪表盘
      Orders.vue         # 订单列表
      OrderDetail.vue    # 订单详情
      Export.vue         # 数据导出
      Notifications.vue  # 通知中心
      Users.vue          # 用户管理
      AuditLog.vue       # 操作日志
    styles/global.css    # 全局样式、CSS变量
  index.html             # HTML模板
  vite.config.js         # Vite构建配置
  package.json           # 前端依赖
```

### 12.2 构建与部署

- **开发模式**：`cd frontend && npm run dev`（Vite开发服务器，端口5173，自动代理API到localhost:3000）
- **生产构建**：`cd frontend && npm run build`（输出到 `public/` 目录）
- **后端启动**：`npm start`（Express提供静态文件服务及API）

### 12.3 状态管理

- 使用 **Pinia** 管理全局状态
- `auth` store：存储登录token、当前用户信息、权限计算属性
- `toast` store：全局消息提示队列
- `modal` store：全局确认弹窗控制

### 12.4 路由说明

- 使用 **Vue Router 4** + `createWebHistory`
- 路由守卫自动拦截未登录用户到登录页
- 生产角色用户登录后自动跳转到订单管理页（无法访问仪表盘）

---

## 十三、API接口一览

### 认证模块
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/auth/login | 用户登录 |
| GET | /api/auth/me | 获取当前用户信息 |
| GET | /api/auth/users | 获取用户列表 |
| PUT | /api/auth/change-password | 修改密码 |

### 部门模块
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/departments | 获取部门列表 |
| GET | /api/departments/tree | 获取部门树 |

### 订单模块
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/orders | 获取订单列表 |
| POST | /api/orders | 创建订单 |
| GET | /api/orders/stats | 统计信息 |
| GET | /api/orders/:id | 获取订单详情 |
| PUT | /api/orders/:id | 更新订单 |
| DELETE | /api/orders/:id | 删除订单 |

### 流程模块
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/orders/:id/stages | 获取订单流程节点 |
| PUT | /api/orders/:id/stages/:stageKey | 更新流程节点状态 |
| PUT | /api/orders/:id/stages/:stageKey/time | 更新时间节点 |

### 导出模块
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/export/orders | 导出订单数据 |
| GET | /api/export/order/:id | 导出单个订单 |

### 通知模块
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/notifications | 获取通知列表 |
| PUT | /api/notifications/:id/read | 标记已读 |

---

*文档版本：v2.4 | 更新日期：2026-07-28*
*文档版本：v2.5 | 更新日期：2026-07-28*

---

## 变更记录

### v2.5 (2026-07-28)
- **流程定义集中管理**：新建 `shared/stage-defs.json`，前后端共用的13个流程阶段定义统一存放。修改流程只需改一处，消除硬编码重复
- **禁止跳过流程阶段**：API 层面禁止从 `pending` 直接跳到 `completed`，必须经过 `in_progress` 状态，确保流程完整可追溯
- **全局错误处理**：Express 新增全局错误处理器，防止未捕获异常导致服务器崩溃
- **权限检查统一化**：`routes/auth.js` 中4处手动角色检查统一替换为 `requireRole('admin')` 中间件，减少遗漏风险
- **生产角色可访问仪表盘**：Dashboard 导航现在对生产角色用户可见

### v2.4 (2026-07-28)
- **SQL注入修复**：数据库层改用 sql.js 原生参数绑定（`prepare/bind/step`），消除手动拼接 SQL 的安全风险
- **后端权限增强**：流程节点状态更新 API 增加部门权限校验，非对应部门用户无法通过 API 直接越权操作
- **通知点击跳转**：通知列表中的通知可点击跳转到对应订单详情，点击时自动标记已读
- **导出加载提示**：导出操作时显示"正在导出"提示，改善用户体验
- **组件去重**：提取 `OrderEditModal.vue` 公共编辑弹窗组件，消除订单列表和订单详情中的重复表单代码

### v2.3 (2026-07-28)
- **页面自动刷新**：订单列表、订单详情、通知中心均添加60秒定时轮询，确保多人协作时数据实时同步

### v2.2 (2026-07-28)
- **密码修改功能**：侧边栏"修改密码"按钮现已可用，跳转到独立的密码修改页面（需验证原密码）
- **代码去重**：提取 `getOverdueInfo` 和 `statusText` 为公共工具函数（`utils/index.js`），消除多组件间的重复代码
- **样式去重**：提取 `.modal-overlay`、`.modal`、`.modal-actions` 等弹窗样式到全局CSS，消除组件间的样式重复
- **路由守卫增强**：`/users` 和 `/audit` 路由增加前端 `adminOnly` 拦截，非管理员用户直接输入URL访问时会被重定向到首页
- **桌面通知**：超期流程检测时同步发送浏览器桌面通知（需用户授权），确保用户不在页面时也能收到提醒

### v2.1 (2026-07-28)
- **移除甘特图**：删除甘特图相关描述及API接口，该功能未实际实现

### v2.0 (2026-07-28)
- **前端框架迁移**：将前端从原生 JavaScript 重构为 Vue 3 + Vite 单页应用
- **新增技术栈**：Vue Router 4（路由管理）、Pinia（状态管理）
- **组件化拆分**：所有页面拆分为独立 Vue 单文件组件（.vue）
  - Layout.vue：主布局与侧边栏导航
  - Dashboard.vue：仪表盘数据展示
  - Orders.vue / OrderDetail.vue：订单管理与详情
  - Users.vue：用户管理
  - Notifications.vue：通知中心
  - Export.vue：数据导出
  - AuditLog.vue：操作日志
- **全局状态管理**：用户认证、Toast消息、Modal弹窗统一使用Pinia管理
- **构建流程**：使用Vite构建，输出到 `public/` 目录，与Express后端无缝集成
- **开发体验**：支持热更新（HMR），开发服务器自动代理API请求


